import styled from '@emotion/styled';

const StyledApp = styled.div``;

const StyledHeader = styled.header``;

const App = () => (
  <StyledApp>
    <StyledHeader>Temp Drive</StyledHeader>
  </StyledApp>
);

export default App;
